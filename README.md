requirements
mcp — The Model Context Protocol SDK (this is the core)
httpx — Modern async HTTP client (for calling cloud APIs)
python-dotenv — Loads environment variables from .env file (for API keys)
pydantic — Data validation and settings management (you'll appreciate this coming from structured COBOL)

